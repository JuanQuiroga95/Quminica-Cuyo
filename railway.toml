[build]
buildCommand = "echo 'Skipping build, using pre-committed .medusa directory'"

[deploy]
startCommand = "cd apps/backend && rm -rf .medusa && cp -r medusa_build .medusa && cp -r .medusa/server/public/admin admin && pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

