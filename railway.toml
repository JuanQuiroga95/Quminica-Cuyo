[build]
buildCommand = "echo 'Skipping build, using pre-committed .medusa directory'"

[deploy]
startCommand = "rm -rf apps/backend/.medusa && mv apps/backend/medusa_build apps/backend/.medusa && pnpm --filter @b2b-starter/backend start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
